#include <stdio.h>
#include "holberton.h"

/**
*_abs - Computes the absolute value of an integer
*@a: an int
*
*Return: tha absolute value of a
*/

int _abs(int a)
{
	if (a >= 0)
	{
	return (a);
	}
else
{
	return (-a);
}
}

